# src/skills/skill_template.yaml
name: "example_skill"
display_name: "Example Skill"
version: "1.0.0"
author: "Your Name"
description: "A brief description of what this skill does."
entry_point: "main.py"
requirements: []
permissions:
  network: false        # 是否允许访问互联网
  filesystem: true      # 是否允许读写文件系统（workspace）
  max_cpu: 0.5          # 最大 CPU 使用率（0.5 = 50%）
  max_memory: 128       # 最大内存（MB）