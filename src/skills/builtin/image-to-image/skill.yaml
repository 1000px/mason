name: image_to_image
version: "2.0.0"
author: Mason Team
description: "图生图：根据脚本批量生成场景图片。当用户说'根据脚本批量生图'、'生成场景图'、'批量生成图片'时激活。"
entry_point: "main.py"
requirements:
  - requests
  - openai
permissions:
  network: true
  filesystem: true
  max_cpu: 0.3
  max_memory: 512