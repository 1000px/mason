name: disk_usage
version: "1.0.0"
author: Mason Team
description: "查看磁盘使用情况。当用户说'磁盘空间'、'硬盘还剩多少'、'磁盘使用'时激活。"
runtime: shell
parameters:
  path:
    type: string
    description: "要查看的目录路径，默认为根目录 /"
    required: false
permissions:
  network: false
  filesystem: true
  max_cpu: 0.1
  max_memory: 32